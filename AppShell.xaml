<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SmartAsthmaAssistane.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SmartAsthmaAssistane"
    xmlns:views="clr-namespace:SmartAsthmaAssistane.Views"
    Title="Smart Asthma Assistant"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Login"
            ContentTemplate="{DataTemplate views:LoginPage}"
            Route="LoginPage" />
    </TabBar>

    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="MainPage" />
    </TabBar>

    <TabBar>
        <ShellContent
            Title="Dashboard"
            ContentTemplate="{DataTemplate views:DashboardView}"
            Route="DashboardView" />
    </TabBar>

</Shell>
